# Image to PDF Converter (Flask + WebView)

This is a web-based **Image to PDF Converter** that allows users to upload images, arrange them in an A4 layout, and download the resulting PDF. The app is built using **Flask** and runs inside an **Android WebView** for mobile use.

## 🚀 Features
- Upload **1-9 images** and arrange them on an A4 sheet.
- Rotate images for better fit.
- Download the generated **PDF**.
- Mobile-friendly UI with **Flask + WebView**.

## 🛠️ Installation & Setup
### 1️⃣ Install Dependencies
```bash
pip install -r requirements.txt
```

### 2️⃣ Run the app
```bash
python app.py
```
The app will run locally at http://127.0.0.1:5000.